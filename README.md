# DirectoryScanner

## Requirements

CMake >= 3.05  
Boost >= 1.68  
Gcc with c++17 support  

## Build

mkdir build  
cd build  
cmake .. && make  

## Usage

Build folder holds server and client exe in respective folders
